## AHC030 memo

- めっちゃ難しい…。
- 正規分布が確率密度関数なので 真値 ± 1/2 で積分した値が真値と同じ確率
- 操作は 2\*N^2 ってことで思ったより少ない
- 1\*1 マス掘って確定されるのが案外強い

- 油田の個数 max 20 個 ⇒ 油田の置き方 400^20 = 2^40 \* 10^40 ≈ 10^50 (AHC にしては探索空間が狭目？)
- 油田は連結 ⇒ 1 つ見つけたらその隣接マスが油田の確率は高い
- 操作コストは 1/sqrt(k) で思ったより変わらないくせに推定が難しくなるので、一旦 1\*1 の操作のみでやる。

- 複数マス占いは、たとえ真値でもどこが油田かまでは特定出来ない (扱いが難しい)
- 複数マス占いは ε も特定も難しい ⇒ とりあえず後回しで貪欲を書きたい

1. 配置パターンを一番搾れるマスを 1\*1 で占い
2. 操作回数は高々 2\*N^2 ⇒ TIME_LIMIT / (残り操作回数) を時間として出来るだけ配置パターン列挙
3. 全列挙出来ない場合は 1 へ、出来た場合はそのパターンが 5 個以下なら全部返答、5 個より大きいなら 1 へ。

⇒ これで暫定 7 位。貪欲強い。(確信)

- DFS の末端を N\*N ではなく、被膜頂点の集合にすれば O(N^2) ⇒ O(∑S) に落ちるのでは？
  ⇒ その程度だとそこまで変わらない ⇒ 本質的な改善としては ε 関係をやるべき

- 1\*1 占いでは結構良いところまで来たので、複数マス占いを考える
- ある 2 マスを占いたい時 1\*1 を 2 回行うより、1\*2 と 1\*1 で行った方が 1-sqrt(2)/2 だけコストがお得 (正しい情報が得られると仮定した場合) ⇒ 拡張して複数頂点対応したが、想像以上にミスるので今のままでは使い物にならない

- 大き目のミノとかは後半は置く場所が 1 つに確定しそう ⇒ そこは枝狩り出来る

- 1\*1 占いで盤面を全探索出来るほどに絞られれば、複数占いをして確率が高い盤面に絞ることは出来そう？

- penalty によってもう少し点数上がりそう。実装的に 0 を見つけた方が選択肢が狭まるようになってるため。( ただ後半ではある程度隣接マスも考慮させたい。 )

- dfs の時に、各マスにおける mino の個数も管理しておいて、oil 1 の癖における mino が無かったらその時点で枝狩り出来そう。

### 得られる情報まとめ

- 各油田の形状

  - 回答と油田の形状を比較して、必ず各油田が埋蔵場所の集合に内包されてるかの確認に使える
  - なんなら最初は 油田の形状で占いをするとかもあり？

- パラメータ ε

  - ε と最尤推定から、占いが何% の確率で正しいかとかは推測出来そう
  - 占いのノイズは、選択した集合のちょうど半分が当たりなら、平均は真の値に近づく
  - 標準正規分布に変数変換 & 正規分布表をハードコード ⇒ 各 x における確率を求めてその確率が ? 以上になる最大の x 今回の推定に使うとか？
  - ε ≈ 0 で分散 kε(1-ε) ≈ kε より、大体 ε ≦ 0.05 は 4 マス とかでも行けそう？
